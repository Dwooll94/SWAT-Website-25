NODE_ENV=development
PORT=3001

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=swat_website
DB_USER=postgres
DB_PASSWORD=your_password_here

# JWT Secret
JWT_SECRET=your_super_secret_jwt_key_here

# Frontend URL
FRONTEND_URL=http://192.168.1.175:3000

# Email Configuration (for user registration/notifications and admin initialization)
# Gmail SMTP Configuration with TLS
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_password_here
EMAIL_FROM=SWAT Team 1806 <noreply@team1806.com>

# Note: For Gmail, you need to:
# 1. Enable 2-Factor Authentication on your Google account
# 2. Generate an App Password (not your regular password)
# 3. Use the App Password as EMAIL_PASS

# Admin Configuration (for automatic admin account initialization)
DEFAULT_ADMIN_EMAIL=admin@smithville.k12.mo.us

# File Upload Configuration
MAX_FILE_SIZE=10485760
UPLOAD_PATH=./uploads

# Security Configuration
BCRYPT_ROUNDS=12

# FIRST API Configuration (for avatar proxy)
# Get credentials from: https://frc-events.firstinspires.org/services/API
FIRST_API_USERNAME=your_username_here
FIRST_API_KEY=your_api_key_here

# SSL/TLS Configuration (Optional - for HTTPS support)
# Generate these certificates and update the paths accordingly
# For self-signed certificates (development):
#   openssl req -x509 -newkey rsa:4096 -keyout private-key.pem -out certificate.pem -days 365 -nodes
# For production, use certificates from a trusted CA like Let's Encrypt
SSL_KEY_PATH=/path/to/your/private-key.pem
SSL_CERT_PATH=/path/to/your/certificate.pem